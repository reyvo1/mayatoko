# Password reset + TOTP 2FA

Adds short-lived single-use password-reset tokens and encrypted TOTP secrets/recovery-code hashes for staff accounts. Plaintext TOTP secrets are returned only during authenticated setup; recovery codes are returned only after confirmation.
