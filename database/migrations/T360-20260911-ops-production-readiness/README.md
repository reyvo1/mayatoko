# T360 Operations / Production Readiness migration

Adds `ApprovalRequest.branchId` so approval queues are tenant/branch scoped even when requests are generated by automation without a requester.

Apply the matching expand SQL before deploying API/worker code. SQLite `ALTER TABLE ADD COLUMN` is not idempotent; run once per database/versioned migration.
